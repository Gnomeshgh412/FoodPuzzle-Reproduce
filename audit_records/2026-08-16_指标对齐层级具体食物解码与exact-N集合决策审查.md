# 2026-08-16 指标对齐层级具体食物解码与 exact-N 集合决策审查

## 1. 审查范围

本轮按 `exploration-experiments/configs/metric_aligned_long_task_preregistration.json` 执行目标对齐重置：

- MFP：train-only latent category posterior + concrete candidate posterior matching；
- MPC：independent RDKit FG demand + food-likelihood-constrained exact-N decoding；
- 保持输入、具体输出、split 和 evaluator 不变；
- 禁止 test、正式 Agent 修改、外部 API/下载、official cache 推理泄漏。

完整报告：

- `exploration-experiments/reports/指标对齐层级解码与exact-N集合决策全面探索报告.md`

## 2. MFP 结论

冻结第十二轮完整 Scientist 候选账本上：

- Reviewer `24/71 = 0.338028`；
- 第一候选 `27/71 = 0.380282`；
- 层级具体候选解码 `31/71 = 0.436620`；
- vs Reviewer：15 wins / 8 losses / 48 ties；
- exact food `1/71`，继续仅作诊断；
- 71/71 输出为具体 ledger candidate，71/71 rationale 至少引用一个 query molecule；
- 10 个 exact-profile stratified group seed 增益 10/10 正，均值 `+0.091182`；
- near-profile 0.90 grouped OOF 增益 `+0.089947`；
- OOF/dev score permutation 均 `p≈0.001`。

Q2 POM/UniMol 可提高 query-category argmax，但不能提高 final concrete decoding；主方法仍为 sparse Q1 soft posterior × C0 candidate-name posterior。

判定：**通过本轮操作性方法门槛，进入下一阶段集成设计候选；不直接修改正式 Agent。**

限制：dev 已多次复用；profile-cluster bootstrap 跨 0；OOF synthetic candidates 与 dev LLM candidates 分布不同；正式 Agent 的候选生成成本尚未复现。

## 3. MPC 结论

### 3.1 H1 父基线上的 S3

独立 RDKit group map 只读取冻结 SMILES，不读取 embedding 数值、DB group 字符串或 judge cache。

S3 Pareto decoder：

- OOF independent FG gain `+0.008572`，CI `[+0.005492,+0.011811]`，5/5 折正；
- OOF molecule macro `+0.003902`，micro 同向，57 wins / 29 losses；
- dev independent FG `+0.014846`；
- 第九轮冻结 cache：`0.649100 → 0.664308`，`+0.015208`，CI `[+0.006618,+0.026379]`；
- official-like wins/losses/ties `15/2/54`；
- 五固定分块全正；
- exact-N 71/71。

删除/负对照：no-food、no-partial、food-only 均低于 full；shuffled demand 降至 `+0.002848` 且 CI 跨 0。

### 3.2 P4 强父基线

从幸存临时脚本恢复 no-UniMol fixed-0.5N P4-family：

- OOF/dev molecule macro `0.718631/0.741192`；
- 历史 `0.71832/0.74072`；
- hybrid-cache official-like `0.666695`；
- 历史 `0.66774`；
- 绝对重建误差 `0.001045`。

S3 `0.664308` 仍低于重建/文档 P4 `0.00239/0.00343`，未满足用户最严格“P4+0.015”标准。

### 3.3 错误驱动后续分支

- P4 bottom-k/next-k metric boundary：independent FG OOF `-0.00389` 至 `-0.00495`，停止；
- P4 defer-capable action verifier：Logistic `-0.00184`、HistGB `-0.00316` independent FG，molecule macro 约 `-0.0155`，停止；
- 两条分支均未打开 dev，未调 threshold 挽救。

判定：**S3 作为相对 H1 的机制候选保留；相对 P4 未通过，不进入正式 Agent。**

## 4. Cache provenance 纠正

当前 project cache 与第九轮冻结 cache 在 405 个共同 H1 分子中有 188 个 group set 不同。用当前 cache 会将 H1 改算为 `0.685689`，不能与历史 P4 比较。

本轮正式配对使用：

- `results/Only-Deepseek/优化实验/第九轮/MPC_ICL高精度核心与H1精确补全/第九轮独立官能团缓存.json`
- SHA-256 `155720cecb72da0f1fa50475bb35ca2fd2c633f190ef516579fa13027c1e352a`

所有 cache 都只在预测冻结后用于评价。 第九轮 cache 对 H1/S3 分别存在 196/197 个空映射 occurrence，配对共享映射但绝对值并非完整化学真值。

## 5. 文件变化

新增：

- `exploration-experiments/code/metric_aligned/*.py` 中本轮 MFP audit、RDKit map、MPC OOF/dev/cache/P4/失败验证脚本；
- `exploration-experiments/results/metric_aligned/mfp/*`；
- `exploration-experiments/results/metric_aligned/mpc/*`；
- 全面报告和本审计。

未修改：

- `code/Only-Deepseek/scientific_agent.py`；
- `code/Only-Deepseek/optimized_agent.py`；
- evaluator；
- test。

## 6. 下一恢复点

### MFP

先在 train grouped-OOF 生成与正式 LLM Scientist 同分布的具体候选账本，再复核 D0；通过后才做干净 Agent 集成设计。若需 Scientist API，必须重新授权。

### MPC

停止继续调 P4 boundary/window/gate。若要超过 P4，下一方向只能是增加可辨识的 food-conditioned missing-group demand，例如 train-only multi-mask Deep Sets/Set Transformer，或经授权的公开 food–molecule/food–FG 关系监督；先在 strict nested OOF 证明 P4 action utility 可识别。
