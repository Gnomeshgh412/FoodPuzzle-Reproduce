# 2026-08-19 长任务九第二阶段资源对齐与外部调用暂停审查

## 1. 结论

本阶段按用户授权冻结 A/B 方案，但没有获得可评价的新 API 结果：

- MFP A0 外部命令在本地持久化修复后，被 Codex automatic approval review/request-validation 层以 `prompt_cache_retention` invalid_parameter 拒绝；命令没有启动，也没有再次请求 DeepSeek；
- MPC ICL pilot 因同一 provider-level 请求阻塞未启动；
- MPC train-only frozen source 本地审计通过；
- dev/test/holdout 均未打开；
- 正式 Agent/evaluator 未修改。

## 2. MFP A0

计划：20 条 train outer-fold query，1 call/query，3 concrete hypotheses，BM25 top-3 outer-fit demos，answer-masked existing evidence，reviewer=0。

状态：

- query plan 已冻结；
- 首次响应未落盘，原因是本地 `numpy.int64` JSON 序列化 bug，已修复；
- 重试时外部命令在执行前被 Codex automatic approval review/request-validation 层拒绝：HTTP 400，param `prompt_cache_retention`；
- 未使用未落盘响应；
- 0 successful persisted responses。

不允许：换 connector、改传输方式绕过同一请求限制、改变 Prompt/模型/预算后继续试探。

## 3. MPC B 本地审计

`local_source_audit_v2.json` 只读 train：

- H1 568/568 exact-N；
- ICL 568/568 有 row，529/568 nonempty；
- ICL 568/568 不含 partial；
- train-only outer retrieval audit 完成；
- 无 ID、fold、partial 泄漏问题。

完全 nested ICL 仍需外部 Scientist generation，尚未执行。

## 4. 结果资格

没有新的 final-output metrics；本阶段不得称为方法进展或方法失败。只可称为：

> **资源对齐方案冻结，外部服务请求层阻塞，MPC 本地 source provenance 通过。**

## 5. 下一恢复点

Codex 自动审批层恢复后，从冻结 A0 未落盘 query 重新运行；不重复计入已落盘成功行。A0 gate 通过后才扩 A1。MPC pilot 遵循同样规则。

下载/训练外部 relation model 仍需单独重新授权。

## 6. 恢复追加审查

用户再次允许恢复后：

- MFP A0 同一命令两次权限审批超时；均未启动脚本、未调用 DeepSeek；停止重试。
- MPC ICL 20 条 pilot 权限审批和 DeepSeek 请求均成功，证明 Key/余额/endpoint 正常。
- MPC pilot parsed 12/20、partial-free unique accepted 8/20，未过 17/20 gate；不扩 568，不打开 dev。
- `N>100` 仅 1/7 parsed，大 N 三份 exact-N JSON 是主要失败机制。
- 新方向是 high-precision single core + deterministic H1/P4 completion，但它改变冻结 Prompt，必须新预注册，不能作为本轮事后修复。
